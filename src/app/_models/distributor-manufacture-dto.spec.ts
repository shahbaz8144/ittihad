import { DistributorManufactureDTO } from './distributor-manufacture-dto';

describe('DistributorManufactureDTO', () => {
  it('should create an instance', () => {
    expect(new DistributorManufactureDTO()).toBeTruthy();
  });
});
