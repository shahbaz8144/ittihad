import { CompanyhierarchyDTO } from './companyhierarchy-dto';

describe('CompanyhierarchyDTO', () => {
  it('should create an instance', () => {
    expect(new CompanyhierarchyDTO()).toBeTruthy();
  });
});
