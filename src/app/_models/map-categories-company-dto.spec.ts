import { MapCategoriesCompanyDTO } from './map-categories-company-dto';

describe('MapCategoriesCompanyDTO', () => {
  it('should create an instance', () => {
    expect(new MapCategoriesCompanyDTO()).toBeTruthy();
  });
});
