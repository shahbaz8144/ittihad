import { ShareDocumentsDTO } from './share-documents-dto';

describe('ShareDocumentsDTO', () => {
  it('should create an instance', () => {
    expect(new ShareDocumentsDTO()).toBeTruthy();
  });
});
