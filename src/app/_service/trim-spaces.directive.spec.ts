import { TrimSpacesDirective } from './trim-spaces.directive';

describe('TrimSpacesDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimSpacesDirective();
    expect(directive).toBeTruthy();
  });
});
